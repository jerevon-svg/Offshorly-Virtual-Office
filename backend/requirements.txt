fastapi>=0.109.0
uvicorn[standard]>=0.27.0

sqlalchemy>=2.0.25
greenlet>=3.0.3  # required by SQLAlchemy's AsyncSession (sync_session.close() bridges through it);
                 # not always auto-resolved as a plain dependency, see sqlalche.me/e/20/xd2s
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.1
psycopg2-binary>=2.9.9  # sync driver used ONLY by Alembic migrations (app runtime uses asyncpg)

pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

httpx>=0.26.0
# [asyncio_client] extra pulls aiohttp/websockets — only exercised by tests/test_chat_socket.py's
# real socketio.AsyncClient, but kept in one requirements.txt to match the scaffold's existing
# single-file convention (pytest/ruff already live here too).
python-socketio[asyncio_client]>=5.11.0

pytest>=7.4.4
pytest-asyncio>=0.23.3
ruff>=0.4.0
