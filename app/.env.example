# "mock" uses in-memory sample Zoho data; "mcp" routes through Zoho MCP
# once connected (currently throws — not implemented). Defaults to "mock"
# if unset or set to an unrecognized value.
VITE_ZOHO_INTEGRATION_MODE=mock
